import React from "react";
import { TimeStamp } from "./TimeStamp";

const Education = () => {
  return (
    <div
      className="h-[100vh] overflow-y-scroll bg-slate-950  flex flex-col md:flex-row justify-center
     items-center  "
    >
      <TimeStamp />
    </div>
  );
};

export default Education;
