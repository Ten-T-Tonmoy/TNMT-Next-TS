import React from "react";
import { TimeStamp } from "./TimeStamp";

const Education = () => {
  return (
    <div
      className="min-h-[100vh] bg-black/60 flex flex-col md:flex-row justify-center
     items-center  "
    >
      <TimeStamp />
    </div>
  );
};

export default Education;
